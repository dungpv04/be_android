services:
  api:
    build: .
    container_name: be_android_api
    ports:
      - 8000:8000
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
    network_mode: bridge
